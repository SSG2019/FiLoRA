[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "filoralib"
version = "0.0.1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["filoralib*"]
